@model IEnumerable<TrackingSystemNew.Models.Shipment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShipmentStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShipmentStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShipmentNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShipmentNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShipmentNo })
        </td>
    </tr>
}

</table>
